# Awkward Arrays

```python
events["pT"].array()  # Returns an Awkward array.
events["pT"].array(library="np")  # Returns a np.array

# Awkward arrays:
muons[0].tolist()

import particle
from hepunits import MeV, GeV
import awkward as ak

ak.argmin()  # Learn about this.
```
